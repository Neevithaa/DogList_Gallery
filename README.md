# 🐶 Dogs List – React Image Grid

A simple and scalable React component that displays a list of dog images using the `array.map()` method. This component demonstrates dynamic rendering, reusable UI patterns, and clean React coding practices.

---

## 🚀 Features

- Dynamic rendering using `map()`
- Data-driven UI with an array of objects
- Clean and minimal JSX
- Proper use of React keys
- Easy to extend and maintain
- Grid-based image layout

---

## 🛠️ Technologies Used

- React JS
- JavaScript (ES6)
- HTML5
- CSS3 (Grid / Flexbox)

---

## 📂 Component Overview

The component stores dog details (image, title, alt text) in an array and dynamically renders each card.

